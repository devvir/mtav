# E2E environment overrides (layered on the root .env).
# No published ports needed; mysql gets a random localhost port if empty.

APP_ENV=testing
DB_HOST=mysql

# Always serve built assets, ignoring any public/hot left by a dev server
VITE_FORCE_BUILD=true

MYSQL_PORT=
