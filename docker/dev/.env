# Development environment overrides (layered on the root .env).
# Personal tweaks go in .env.local (gitignored), not here.

APP_ENV=local
DB_HOST=mysql

NGINX_PORT=8000
VITE_PORT=5173
MYSQL_PORT=3307
MAILHOG_SMTP_PORT=1025
MAILHOG_HTTP_PORT=8025
REVERB_PORT=8080
