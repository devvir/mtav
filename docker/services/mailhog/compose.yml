# Mail testing server (SMTP sink + web UI).
#
# Ports empty ⇒ random host ports.
services:
  mailhog:
    image: mailhog/mailhog
    restart: unless-stopped
    ports:
      - "${MAILHOG_SMTP_PORT:-}:1025"
      - "${MAILHOG_HTTP_PORT:-}:8025"
