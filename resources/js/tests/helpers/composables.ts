// Copilot - Pending review
import { vi } from 'vitest';

/**
 * Mock common composables - called from setup.ts at module level
 * Do NOT call this from tests
 */
export function setupComposableMocks() {
  // These are already handled by vi.mock at module level in setup.ts
}
